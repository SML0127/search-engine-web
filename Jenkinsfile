pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh '''
                echo "Start Build"
                echo "npm run build"
                '''
            }
        }
    }
}
