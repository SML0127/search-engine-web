import { NodeEditor } from 'rete';
declare type Params = {
    createAndConnect?: boolean | {
        keyCode: string;
    };
    pickConnection?: boolean | {
        keyCode: string;
    };
};
declare function install(editor: NodeEditor, params: Params): void;
declare const _default: {
    name: string;
    install: typeof install;
};
export default _default;
